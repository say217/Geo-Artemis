[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "geo-artemis"
version = "0.1.0"
description = "Geo Artemis Application - Geospatial Intelligence Platform"
authors = [
    { name = "Geo Artemis Team" }
]
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["Main"]
